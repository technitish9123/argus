{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "uniswap_v3.exactInputSingle",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "protocol": { "const": "uniswap_v3" },
    "contract": {
      "anyOf": [
        { "const": "router" },
        { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" }
      ]
    },
    "method": { "const": "exactInputSingle" },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tokenIn",
        "tokenOut",
        "fee",
        "recipient",
        "deadline",
        "amountIn",
        "amountOutMinimum",
        "sqrtPriceLimitX96"
      ],
      "properties": {
        "tokenIn": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "tokenOut": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "fee": { "type": "integer", "enum": [500, 3000, 10000] },
        "recipient": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "deadline": { "type": "integer", "minimum": 0 },
        "amountIn": { "type": "string", "pattern": "^[0-9]+$" },
        "amountOutMinimum": { "type": "string", "pattern": "^[0-9]+$" },
        "sqrtPriceLimitX96": { "type": "string", "pattern": "^[0-9]+$" }
      }
    },
    "chainId": { "type": "integer", "minimum": 1 },
    "x-abi": {
      "type": "string",
      "const": "exactInputSingle((address tokenIn,address tokenOut,uint24 fee,address recipient,uint256 deadline,uint256 amountIn,uint256 amountOutMinimum,uint160 sqrtPriceLimitX96))"
    },
    "x-spenderRole": { "const": "router" },
    "x-debits": {
      "description": "Approval/debit hints for the SDK",
      "x-hints": [{ "amountKey": "amountIn", "tokenParam": "tokenIn" }]
    }
  },
  "required": ["protocol", "contract", "method", "params"]
}
