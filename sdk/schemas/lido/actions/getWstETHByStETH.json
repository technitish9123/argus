{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "lido.getWstETHByStETH",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "protocol": { "const": "lido" },
    "contract": { "const": "wsteth" },
    "method":   { "const": "getWstETHByStETH" },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stETHAmount"],
      "properties": { "stETHAmount": { "type": "string", "pattern": "^[0-9]+$" } }
    },
    "chainId": { "type": "integer", "minimum": 1 },
    "x-abi":   { "type": "string", "const": "function getWstETHByStETH(uint256 stETHAmount) view returns (uint256)" }
  },
  "required": ["protocol","contract","method","params"]
}
