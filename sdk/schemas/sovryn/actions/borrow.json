{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sovryn.borrow",
  "type": "object",
  "properties": {
    "protocol": { "const": "sovryn" },
    "contract": { "const": "iUSDT" },
    "method":   { "const": "borrow" },
    "params": {
      "type": "object",
      "required": ["amount"],
      "properties": {
        "amount": { "type": "string", "pattern": "^[0-9]+$" }
      }
    }
  },
  "required": ["protocol","contract","method","params"],
  "x-abi": "function borrow(uint256 borrowAmount) external returns (uint256)"
}
