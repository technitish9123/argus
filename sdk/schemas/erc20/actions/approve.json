{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "erc20.approve",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "protocol": { "const": "erc20" },
    "contract": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
    "method": { "const": "approve" },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["spender", "amount"],
      "properties": {
        "spender": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "amount":  { "type": "string", "pattern": "^[0-9]+$" }
      }
    },
    "chainId": { "type": "integer", "minimum": 1 }
  },
  "required": ["protocol","contract","method","params"],
  "x-abi": "function approve(address spender, uint256 amount)"
}
