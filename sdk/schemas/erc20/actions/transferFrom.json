{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "erc20.transferFrom",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "protocol": { "const": "erc20" },
    "contract": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
    "method": { "const": "transferFrom" },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from","to","amount"],
      "properties": {
        "from":   { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "to":     { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "amount": { "type": "string", "pattern": "^[0-9]+$" }
      }
    },
    "chainId": { "type": "integer", "minimum": 1 }
  },
  "required": ["protocol","contract","method","params"],
  "x-abi": "function transferFrom(address from, address to, uint256 amount)"
}
