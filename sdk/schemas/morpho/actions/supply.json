{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "morpho.supply",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "protocol": { "const": "morpho" },
    "contract": {
      "anyOf": [
        { "const": "morpho" },
        { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" }
      ]
    },
    "method": { "const": "supply" },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "required": ["market", "asset", "assets", "onBehalf", "receiver"],
      "properties": {
        "market": { "type": "string", "pattern": "^0x[a-fA-F0-9]{64}$", "description": "bytes32 MarketId" },
        "asset": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$", "description": "loan token" },
        "assets": { "type": "string", "pattern": "^[0-9]+$" },
        "onBehalf": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" },
        "receiver": { "type": "string", "pattern": "^0x[a-fA-F0-9]{40}$" }
      }
    },
    "chainId": { "type": "integer", "minimum": 1 },

    "x-abi": {
        "type": "string",
      "const": "function supply((address loanToken,address collateralToken,address oracle,address irm,uint256 lltv) marketParams,uint256 assets,uint256 shares,address onBehalf,bytes data)"
    },

    "x-spenderRole": {"type": "string", "const": "morpho"},
    "x-debits": {
      "x-hints": [
        { "amountKey": "assets", "tokenParam": "asset" }
      ]
    }
  },
  "required": ["protocol", "contract", "method", "params"]
}
